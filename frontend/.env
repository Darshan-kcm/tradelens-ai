# Leave unset for local dev and single-origin deployments (relative /api via
# the Vite proxy). For split deployments (e.g. frontend on Vercel, backend on
# Render), set this to your deployed backend's origin, no trailing slash:
# VITE_API_URL="https://tradelens-backend.onrender.com"
